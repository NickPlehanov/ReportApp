<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewModels="clr-namespace:ReportApp.ViewModels"
             xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid" xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="ReportApp.MainPage" IconImageSource="@drawable/icon">
    <ContentPage.Resources>
        <viewModels:MainPageViewModel x:Key="viewModel"/>
    </ContentPage.Resources>
    <Grid BindingContext="{StaticResource viewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="CenterAndExpand">
            <DatePicker Date="{Binding StartDate}">
                <DatePicker.Format>d</DatePicker.Format>
                <DatePicker.MinimumDate>
                    <sys:DateTime x:FactoryMethod="Parse">
                        <x:Arguments>
                            <x:String>Jan 1 2010</x:String>
                        </x:Arguments>
                    </sys:DateTime>
                </DatePicker.MinimumDate>
                <DatePicker.MaximumDate>
                    <sys:DateTime x:FactoryMethod="Parse">
                        <x:Arguments>
                            <x:String>Dec 31 2050</x:String>
                        </x:Arguments>
                    </sys:DateTime>
                </DatePicker.MaximumDate>
            </DatePicker>
            <TimePicker Time="{Binding StartTime, Mode=TwoWay}">
            </TimePicker>
        </StackLayout>
        <StackLayout Grid.Row="0" Grid.Column="1" HorizontalOptions="CenterAndExpand">
            <DatePicker Date="{Binding EndDate}" >
                <DatePicker.Format>d</DatePicker.Format>
                <DatePicker.MinimumDate>
                    <sys:DateTime x:FactoryMethod="Parse">
                        <x:Arguments>
                            <x:String>Jan 1 2010</x:String>
                        </x:Arguments>
                    </sys:DateTime>
                </DatePicker.MinimumDate>
                <DatePicker.MaximumDate>
                    <sys:DateTime x:FactoryMethod="Parse">
                        <x:Arguments>
                            <x:String>Dec 31 2050</x:String>
                        </x:Arguments>
                    </sys:DateTime>
                </DatePicker.MaximumDate>
            </DatePicker>
            <TimePicker Time="{Binding EndTime, UpdateSourceEventName=PropertyChanged}">
                <TimePicker.Format>t</TimePicker.Format>
            </TimePicker>
        </StackLayout>
        <StackLayout Grid.Row="0" Grid.Column="2" HorizontalOptions="CenterAndExpand" WidthRequest="50" VerticalOptions="CenterAndExpand">
            <Entry Text="{Binding Late}" Keyboard="Numeric" HorizontalTextAlignment="Center" />
            <!--<Label Text="{Binding Msg}" HorizontalOptions="CenterAndExpand" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" VerticalOptions="CenterAndExpand"/>-->
        </StackLayout>
        <!--<Button Grid.Row="0" Text="Загрузить данные" Command="{Binding GetReport}"/>-->
        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <dg:DataGrid SortedColumnIndex="0" BorderColor="#620000" x:Name="dataGrid" IsRefreshing="{Binding IsBusy, Mode=TwoWay}" ItemsSource="{Binding reportAlarmExBases, UpdateSourceEventName=PropertyChanged}" SelectedItem="{Binding SelectedReportAlarmExBase, Mode=TwoWay}" PullToRefreshCommand="{Binding GetReport}">
                <dg:DataGrid.Columns>
                    <dg:DataGridColumn Width="*" Title="Время сработки" PropertyName="new_alarm_dt"/>
                    <dg:DataGridColumn Width="2*" Title="Объект" PropertyName="new_obj"/>
                </dg:DataGrid.Columns>
                <dg:DataGrid.RowsBackgroundColorPalette>
                    <dg:PaletteCollection>
                        <Color>#F2F2F2</Color>
                        <Color>#FFFFFF</Color>
                    </dg:PaletteCollection>
                </dg:DataGrid.RowsBackgroundColorPalette>
                <!--<dg:DataGrid.Resources>
                    <ResourceDictionary>
                        <conv:StreakToColorConverter x:Key="StreakToColorConverter"/>
                    </ResourceDictionary>
                </dg:DataGrid.Resources>-->
            </dg:DataGrid>
        </Grid>
        <!--<StackLayout Grid.Row="2">
        </StackLayout>-->
    </Grid>


</ContentPage>
